Colocar aquí el binario compilado:
  yola-daemon-mobile (ARM64)

Compilar con:
  cd daemon && cargo build --release --target aarch64-linux-android

El APK extraerá este binario en tiempo de ejecución.
